default: &default
  store_options:
    max_age: <%= 60.days.to_i %>
    max_size: <%= 10.gigabytes %>
    namespace: <%= Rails.env %>

development:
  <<: *default

test:
  <<: *default

uat:
  database: cache
  <<: *default

production:
  database: cache
  <<: *default
